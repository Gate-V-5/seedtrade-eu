{
  "name": "seedtrade-eu",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "python3 -B -m unittest discover -s tests -p 'test_*.py' -v",
    "build:vite": "vite build && python3 -B scripts/prerender_seo.py && node scripts/prerender_static_first.mjs",
    "build": "npm run build:vite",
    "preview": "vite preview"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^6.1.1",
    "vite": "^8.2.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {}
}
